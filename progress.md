# Progress Log — MouseDroidAGI

Reverse chronological (newest on top). Set the date with `date +%F`; never copy a
literal date. Rotation: keep ~10 sessions; move older entries to
`progress-archive/YYYY-QN.md`. See HARNESS_SPEC.md §11.

## 2026-08-23 — Session 011
**Features worked:** F-030 (doc reconciliation, new — introduced `in_progress`); cross-cutting hygiene sweep riding the same branch/PR (no F-number, same precedent as Session 006's PR #178 sprint).
**Status changes:** F-030 introduced `in_progress`, `implemented_in: null` (pins to the squash SHA on closeout, per convention). No other catalog status changes.
**Structural changes — F-030, six verified doc drifts, all fixed:** `CLAUDE.md` / `docs/claude/surfaces/ci-gates.md` / `docs/claude/surfaces/README.md` claimed a "12-job" CI pipeline naming six jobs that do not exist (`secret-scan`, `skills`, `test-fast`, `validate`, `regression`, `package`); corrected to the real 16-job breakdown sourced from `ci.yml`'s own `# Stage N` comments. `src/mousedroid/orchestrator/CLAUDE.md` — the module map anyone reads before touching the orchestrator — named `RobotOrchestrator`, `state.py`, `factory.py:_build_orchestrator`, `ConstitutionalSafetyMonitor`; grep confirmed none exist in any `.py` file. Rewritten around the real symbols: `orchestrator.py::MouseDroidOrchestrator`, `autonomous.py::AutonomousOrchestrator` (zero production callers, stated explicitly), `safety/monitor.py::MouseDroidSafetyMonitor`. `HARNESS_SPEC.md` (3 sites), `tests/agent.md`, `docs/architecture/c4-spec-harness.md` said 85% coverage for `src/mousedroid`; real gate is 90% (`pyproject.toml`'s `fail_under`) — 85% is correct only for the separate `tools/claude_hooks/` gate, now stated as such. `docs/CHARTER.md` §5 said `growth/` was "not yet instantiated"; `build_growth_coordinator` is called at `factory.py:4308` (default-OFF, metrics-wired, M6 posture) — split the sentence rather than overcorrecting, since `meta/`/`scaling/` genuinely have no builder. Root `NEXT_STEPS.md` corrected "Phase 5 deferred" to "landed", citing the real `sim/mujoco_rover_env.py::RoverMuJoCoEnv`.
**Self-caught follow-on — the growth/-unwired claim was live in four more places.** Applying `narrative-correction-sweep` to my own §5 fix rather than trusting it generalized: `docs/CHARTER.md` §1 and §3 both still listed `growth/` as "not yet wired", contradicting the already-corrected §5 in the same file; `README.md`'s cognitive-stack table had it in the wrong bucket outright; `docs/architecture.md`'s Level 3d section cited it as a same-shape precedent for genuinely-unwired GCP components; `NEXT_STEPS.md` item 0b repeated that citation even though a later item in the same file already said growth "is now wired". All four fixed; README.md gained a third bucket ("Factory-instantiated, default-OFF pending a soak decision") rather than forcing growth/ into either existing table.
**Structural changes — SKILLS.md/AGENTS.md reconciliation + two catalog-wide pins:** SKILLS.md's `## Subagent skills` table named 7 fictional agent names; rewritten to the real 7 `.claude/agents/` names. New `## Workforce skills` section documents every real `.claude/skills/` directory not already covered elsewhere — the two-shape convention (documented procedure vs. invocable directory) turned out to be deliberate design; the real drift was the opposite direction, real skills missing from the index. New `test_every_skill_directory_is_mentioned_in_the_index()` + `test_every_agent_is_listed_in_the_subagent_skills_table()` in `test_claude_workforce_aqa.py`, both proven red against the original defects before being restored (37/37 pass in that file). New `tests/regression/test_doc_reconciliation_aqa.py` pins the CI job count and coverage-floor numeric claims against their source of truth — both of its own regexes produced false positives against my own legitimate text on first write (a true "5 jobs run *(advisory)*" claim; the sprint's own correctly-qualified "85% for `tools/claude_hooks/`" text) and were tightened before being trusted. New `scripts/validations/F-030.sh` (5 passed).
**Structural changes — `_DOC_GLOBS` had the identical shape of gap it was reconciling doc claims against:** `test_ci_gate_wiring_aqa.py`'s `TestOrphanTierNarrativeAccuracy` sourced its file list from a 5-pattern directory-prefix roster (`*.md`, `docs/**/*.md`, `openspec/**/*.md`, `.claude/**/*.md`, `src/**/*.md`) that silently missed **15 git-tracked `.md` files** — including `tests/agent.md`, one of this same sprint's own drift-fix targets, so the gap was not hypothetical (measured: `git ls-files "*.md"` returns 217, the old glob roster caught 202). Replaced with a `git ls-files -- "*.md"`-sourced `_tracked_docs()`, matching the `_git_tracked` idiom already used in `test_claude_workforce_aqa.py` — no directory list to maintain, cannot drift behind a new subsystem doc. New skill `.claude/skills/narrative-correction-sweep/SKILL.md` documents the whole-file, whitespace-normalized, `git ls-files`-sourced sweep procedure this exact sprint used twice (the F-028/F-029 round-4 "only coverage" claim, and this `_DOC_GLOBS` gap); validated clean by `tools/validate_skill_commands.py` and added to SKILLS.md's workforce-skills table.
**Structural changes — three hardcoded-value fixes from a delegated config-guardian audit:** (1) `orchestrator._publish_telemetry` never threaded `Settings.telemetry.vision_feature_max_samples` into `build_telemetry_frame(...)` — a previously dead config field; wired, with a new test proving the cap is honoured (proven red pre-fix: an 8-element mock array passed through uncapped against `assert 8 == 3`). (2) `validation/runtime/_storage.py::_resolve_pcie_ssd_mount` read a hardcoded env var name (`MOUSEDROID_SSD_MOUNT`); sourced from a new schema field `ExperienceConfig.ssd_mount_override_env_var` (same default, backwards-compatible — all 44 pre-existing tests pass unchanged), with a new test proving the env var *name* is itself schema-configurable (45/45 pass). (3) `comms/_utils.py`'s `MAX_PWM` + three `ESP32_CMD_TYPE_*` constants gained `# hardcoded-ok` markers matching `command_set.py`'s existing treatment of the same class of vendor-protocol constant; `.claude/workforce.yaml`'s `hardcoded_ok` ratchet ceiling raised 24→28 (verified real count exactly 28) with a comment noting this is marker-treatment consistency, not new suppressions.
**Bug found and fixed — `scripts/archive_stale_branches.sh` origin/HEAD symref (the most consequential finding this session):** `git branch -r --format='%(refname:short)'` renders a configured `refs/remotes/<remote>/HEAD` symref as the BARE remote name (`origin`), not `origin/HEAD` — a real git behaviour, reproduced on a plain fresh `git clone`, not repo-specific. The existing `grep -v '^HEAD$'` filter does not catch this rendering, so the bare token reached `$REMOTE/$b` as the unresolvable `origin/origin`: a stray `fatal:` in dry-run output, and under `--push`, `git tag -f archive/origin origin/origin` fails at **exit 128** before any real branch is touched — unusable on any standard fresh clone, which is this script's entire reason for existing (bulk cleanup of ~88 stale branches). **This was introduced by this same plan's own round-5 fix to this file** (`3dc067c`, Session 010) and shipped unnoticed because the fixture used to develop that fix happens not to trigger it (its pinned commit lives off the branch `origin/HEAD` resolves to). Caught by a delegated test-engineer subagent's adversarial audit; independently reproduced end-to-end in three disposable local git fixtures (never against the real GitHub remote) before trusting it. Fixed via a shared `_remote_branches()` helper applied at both the pin-carrier-detection call site and the main enumeration loop — the latter had the identical latent defect, the same instance-vs-class pattern this whole plan keeps finding. New `TestOriginHeadSymrefHandling` in `tests/unit/scripts/test_archive_stale_branches.py` (a `repo_with_stale_orphan_branch` fixture that lets a plain `git clone` set `origin/HEAD` itself, not a hand-crafted symref) proves it end-to-end, including a full `--push` round-trip that actually archives-as-tag and deletes a genuinely stale branch. **Proven red against the pre-fix script first**: temporarily swapped in HEAD's pre-fix content, 2 of 3 new tests failed exactly as predicted (the stray `fatal:`, the exit-128 death), the premise-guard test correctly still passed, then the fix was restored byte-identical (diffed to confirm).
**Explicitly deferred, recorded in the F-030 openspec bundle's `tasks.md` with rationale, not silently dropped:** mock Hailo-8 output shapes (`hailo_runtime.py::MockHailoRuntime.DEFAULT_OUTPUT_SHAPES`) staying class constants rather than moving to `HailoConfig` schema — already overridable via constructor injection, values are intrinsic to a specific YOLO architecture's output shape rather than an operational tunable. **Self-correction, caught by the openspec-author agent, not by me:** my first-draft rationale claimed `hardware/` was an exempt `check_no_hardcoded_values.py` directory prefix; the agent checked `ALLOWED_DIR_PREFIXES` directly and found it names exactly four prefixes (`config/schema/`, `telemetry/metrics/`, `telemetry/server/`, `validation/runtime/`) — `hardware/` is not among them. Real reason: the file is untouched by any diff this sprint, so the changed-lines-only scanner never reaches it. `prove_pin_fails.sh`'s `--paths`/`--tests` unquoted word-splitting on glob characters and space-containing parametrized pytest IDs — dev/CI-only tool with 10 passing tests already (`tests/unit/scripts/test_prove_pin_fails.py`; corrected from an earlier miscount of 6), narrow edge cases, clear-error rather than silent-wrong failure mode. A `post_edit` shellcheck hook and a `regression_pin_reminder` hook — `shellcheck` is not installed in this dev container and is not wired into CI at all today (verified: zero matches beyond inline disable-comments across `ci.yml`/`ci.sh`/the Makefile); either hook needs new `tools/claude_hooks/` test coverage under the dedicated `make hooks` gate.
**Validation evidence:** `make gates` clean (ruff, `mypy --strict` on 384 `src/` files + 12 `tools/claude_hooks/` files, skill validator, `validate.py --tier fast` 22 done/20 ran, hardcoded-value gate, settings-identity check). `make test` **6126 passed, 55 skipped, coverage 93.49%** vs the 90% gate. `make regression` 826 passed, 2 skipped. `make behaviour` 17 passed. `bash scripts/ci.sh` run twice: first run hit a non-reproducible Hypothesis `filter_too_much` health-check flake in `test_clamp_saturates_below_lower` (untouched file, passed 3/3 in isolation, passed on the second full run — confirmed non-deterministic, not a regression); second run hit `test_mock_vla_instrumentation_within_budget` at 1.20x vs. a 1.15x budget — reproducible 3/3 in isolation on this container, matches `.github/advisory_stages.yaml`'s own `performance`-job rationale verbatim ("noisy on shared runners... 1.17x vs a 1.15x budget from contention alone"), an advisory `continue-on-error` job in real CI, and touches no file changed this session. Neither failure is a regression from this work; both are named here rather than omitted. `python tools/validate_skill_commands.py` — OK, all skill commands valid, including the new `narrative-correction-sweep`. `python -m pytest tests/regression/test_ci_gate_wiring_aqa.py tests/regression/test_doc_reconciliation_aqa.py tests/regression/test_claude_workforce_aqa.py tests/unit/scripts/test_archive_stale_branches.py -q --no-cov` — 68 passed.
**CodeRabbit review round on PR #202 (10 findings, same session, all verified against the tree before fixing):** the one tagged Major — "F-030 does not execute all declared governance checks" — split cleanly into a tractable half and a genuinely hard half. The tractable half (orchestrator-symbol existence) shipped as `test_orchestrator_claude_md_names_only_real_symbols`, proven red against the pre-fix doc content. The hard half (the `growth/`-wiring-claim repetition) got a real attempt — a sentence-scoped sweep across the four live target files, built and run against the tree exactly as `narrative-correction-sweep`'s own guardrails prescribe — and it produced two false positives on the first real run: a `README.md` code-fence file listing swallowed whole by period-boundary splitting, and a legitimate Session-009 `NEXT_STEPS.md` sentence using none of the pin's qualifier words. Deferred honestly rather than shipped fragile, with the full account in the F-030 bundle's `tasks.md`. The other 9 findings were all real, all fixed: a regex-vs-fixed-string bug in `archive_stale_branches.sh`'s `grep -e "$REMOTE"` (same script this session already fixed once for the origin/HEAD bug — a second, independent defect in the same few lines); `check=False` on `_tracked_docs()`'s `git ls-files` call, which would have let a git failure read as "zero live docs, sweep vacuously passes" — exactly the "a pin that cannot fail is worse than no pin" class this whole plan cares about; an unbounded `table_text` slice in the agent-roster pin; the coverage-floor pin's hardcoded `"85%"` search redesigned to compare against `_real_src_coverage_floor()` directly (tuned against `c4-spec-harness.md`'s unrelated "100% on spec.py" claim before trusting it); README's `growth/` distillation objective corrected from "KL+CE" (the class's default) to the actually-wired "regression" (MSE) objective (`factory.py` passes `objective="regression"`); `NEXT_STEPS.md`'s roadmap-intro sentence still said "Phases 5 and 6 are deferred" one paragraph above the already-corrected Phase 5 entry — the identical instance-not-class miss this whole sprint kept finding, this time caught by the bot instead of the sweep; `pin-reachability-audit`'s all-features audit read local refs without fetching first; the F-030 bundle's own `tasks.md` quality-gate fence had no language tag and claimed a gate no early task could satisfy. `make gates` clean, `make regression` 827 passed/2 skipped (+1 for the new pin) after all fixes.
**Next:** F-031 disposition ADR (already re-scoped per Session 010's peer review — `AutonomousOrchestrator` stays off the production path); F-032 GCP observability wiring (needs F-029, already landed); F-033 chassis safety sensors (needs a rover bench session); F-035 deploy re-pin ritual, now scoped to cover all 16 `implemented_in` pins, not just the deploy manifest.

## 2026-08-23 — Session 010
**Features worked:** F-028, F-029 (post-merge `implemented_in` re-pin only — no behavioural change).
**Status changes:** none. Both were already `done`; only their provenance pins move.
**Why:** PR #201 merged as **squash `9bd3dc7`**, so none of its 11 branch commits reached the
integration branch. F-028 pinned `df928d9` and F-029 pinned `5c2f044`, which the squash did not
preserve — leaving both reachable only from `claude/jetson-rover-e2e-next-steps-00jwqk`. The
harness resolves `implemented_in` with `git rev-parse` (`src/mousedroid/harness/spec.py:185`), an
**existence** check, so both passed while that branch lived and would have started failing
`validate.py --strict-git` the moment it was deleted (`scripts/validate.py:37` documents
unresolved refs as *errors*, `:71` turns errors into `VALIDATION FAILED`). Re-pinned to the squash
— which is the catalog convention: F-015..F-020 pin `a12c95b` (PR #151's squash), F-021 pins
`40a6399` (PR #153's).
**Verified rather than assumed:** F-022 (`27b5233`), F-023 (`e730a0a`) and F-027 (`cb2d724`) were
checked individually and *are* ancestors of the merged base — no action needed. Five older pins
(F-002/004/005/006/007) are not on the integration branch but exist on 32/3/6 other remote
branches respectively, so `rev-parse` resolves them; they are covered by the pin-carrier
protection rather than at risk. The `[warn] not a resolvable git ref` lines seen earlier in this
repo were a shallow-clone artifact and disappear once the base is fetched.
**A correction, and a gap the round-5 fix does NOT close.** I first wrote that
`scripts/archive_stale_branches.sh` — generalised in `3dc067c` to protect `features.yaml` pin
carriers — had saved these pins. It did not. GitHub's **delete-branch-on-merge** removed
`claude/jetson-rover-e2e-next-steps-00jwqk` at merge time, and that path never runs the archive
script. Measured after the fact: `df928d9` and `5c2f044` are contained by **zero** remaining
remote branches, so a fresh clone cannot resolve either and `--strict-git` was already failing for
both features by the time this was noticed. This re-pin is a repair, not a precaution.
The archive-script generalisation remains correct and still protects the *cleanup* path — but the
protection is not universal, and believing it was is the same error as the original "the script
protects pin carriers" note it replaced. **Consequence for F-035:** the annotated-tag ritual must
run **before** merge, or delete-branch-on-merge must be disabled for pin-carrying branches.
Protection that only covers one of two deletion paths is a partial guard described as a total one.
**Next:** F-030 (doc reconciliation). Its target list was re-verified at `9bd3dc7` — see the handoff
plan; note the CI job count is **16**, not 12, six of the twelve job names in CLAUDE.md do not
exist, and the real safety class is `MouseDroidSafetyMonitor` (an earlier correction said
`SafetyMonitor`, which is also wrong).

## 2026-08-22 — Session 009
**Features worked:** F-022, F-023, F-027 (closeout: in_progress → done); F-028 (new — CI tier completeness).
**Status changes:** F-022 → done (`implemented_in` = `27b52338d6ddb2f6c882518f0fa78bd50a593101`, PR #172); F-023 → done (`e730a0a3fd14f66f1d39520abc67f5d153e3ddc4`, PR #173); F-027 → done (`cb2d7246890c5246cf9f8ab721338b0e63d97c7f`, PR #187); F-028 → done (`df928d9…`) and F-029 → done (`5c2f044…`) — pinned to the commits that actually contain the work. An earlier cut pinned both to the branch-point `175606b0…`, which contains neither, citing an "F-015..F-021 branch-point convention" that does not exist. The real convention is the opposite: F-015..F-020 pin `a12c95b` (PR #151's squash merge) and F-021 pins `40a6399` (PR #153's) — the commit each feature actually landed in. Both claims corrected in `6f459a5`. Consequence for this PR: `df928d9`/`5c2f044` are branch commits that a squash merge will not preserve, so they need re-pinning to the squash SHA post-merge — which is the convention, not an exception to it.
**Structural changes:** Three test tiers — `tests/functional/`, `tests/user_journey/`, `tests/security/` — reached ZERO CI paths (absent from `scripts/ci.sh`, `.github/workflows/ci.yml` and every Makefile target), the same rot the smoke tier suffered before PR #178. Wired into the blocking `test` job and into `ci.sh` **outside** the `MOUSEDROID_CI_SLIM` gate (~2.5s total), deliberately NOT into the `ci.yml` job named `security` — that job is `continue-on-error` and would have swallowed every failure. New `TestOrphanTierWiring` in `tests/regression/test_ci_gate_wiring_aqa.py` pins both wirings plus the negative. New `scripts/validations/F-028.sh`. `openspec/project.md` gained a **landed** column (the `openspec-change` skill requires a landing SHA; the table had nowhere to record one) and its three prior rows moved to `implemented`.
**Peer-review corrections that changed the design:** (1) `tests/regression/test_alayaworld_memory_distill_backwards_compat.py` hard-pinned `f023["status"] == "in_progress"` — a lifecycle snapshot masquerading as a schema property, which failed the instant F-023 closed. Replaced with the durable invariant: status in-vocabulary, and a `done` entry carries a 40-hex `implemented_in` plus a non-empty `validation_command`. (2) The obvious home for `tests/security` — the job named `security` — is a trap; it is advisory, so the pin asserts that precondition *before* asserting the negative, making a future promotion surface as a rethink rather than a silent pass.
**Validation evidence:** `scripts/validate.py --tier fast` → **OK: 20 done; ran 18**. Feature commands re-run individually: F-022 55 passed, F-023 94 passed/3 skipped, F-027 453 passed. Orphan tiers 17 passed in 2.44s. `TestOrphanTierWiring` proven to fail by reverting the wiring in both files (3 of 4 assertions red), then restored. `bash scripts/validations/F-028.sh` exits 0. `features.yaml` validates against `features.schema.json` (22 features). Note: `--strict-git` warns on F-002/004/005/006/007 in this **shallow clone** (52 commits) — pre-existing, not introduced here; the nightly uses `fetch-depth: 0`.
**F-029 (same session):** `GCPLoggingConfig.enabled` and `GCPMonitoringConfig.enabled` flipped `True` → `False` (`GCPFirestoreConfig` already defaulted `False` — the asymmetry was the bug). `Settings.gcp` is `Optional` and defaults `None`, so nothing egressed on a stock config; the real hazard was an operator adding a minimal `gcp:` block for one reason (Firestore, storage) and silently enabling **two** off-device egress channels they never named. Regression pair `test_gcp_egress_defaults_aqa.py` (8) + `test_gcp_egress_defaults_backwards_compat.py` (5), proven to fail by reverting both defaults (3 red), then restored. The AQA deliberately asserts the *partial-block* scenario — asserting "no egress by default" would be vacuous, true before and after. `tests/unit/cloud/test_config_backwards_compat.py::test_settings_with_gcp_project_id` asserted the old `True`/`True`/`False` — that test was itself the proof the hazard was real; updated with the rationale inline. Behaviour changes for no shipped config: `config/gcp_digital_twin.yaml` sets all three explicitly, pinned against both the parsed model and the raw YAML so a future edit cannot quietly start leaning on the default. F-029 gates F-032. Evidence: `make gates` clean; `validate.py --tier fast` **OK 22 done / 20 ran**; regression 803 passed; `make test` 6108 passed, coverage 93.51%.
**Peer-review round (3 agents: peer-reviewer, test-engineer, config-guardian) — REQUEST_CHANGES, all fixed:** (1) **F-029 was half-done and its own verification bullet was false.** `GCPPubSubConfig`/`GCPStorageConfig` had no `enabled` field at all, and `build_cloud_telemetry_sink`/`build_cloud_experience_exporter` gated on `cfg.gcp is None` alone — so a partial `gcp:` block still built a live Pub/Sub sink and GCS exporter. Those two are the *wired* ones; logging/monitoring have no consumer yet, making the original narrative overstated. Both now carry `enabled=False` and both builders are gated, with `_log.info("cloud_*_disabled", reason=...)` so a disabled sink is not a silent `None`. `config/gcp_digital_twin.yaml` opts in explicitly, preserving its behaviour. (2) **`implemented_in` for F-028/F-029 pointed at `175606b`, the branch point** — a commit that does not contain either change. Resolvable-but-wrong passes every gate silently. Re-pinned to the real commits; the cited "F-015..F-021 branch-point convention" was also false (they pin `a12c95b`, their actual landing commit) and is corrected. (3) **"tests/security/ is the only coverage of the pre-egress RegexInjectionFilter" was false** and had been propagated to 5 places — `tests/unit/security/test_injection_filter.py` holds 11 tests that already ran. Corrected everywhere: this closes a *wiring* gap, not a coverage hole. (4) **`test_ci_sh_runs_all_three_tiers` was satisfiable by a comment** — it matched raw file text, and this diff's own comment names the tiers. Now matches comment-stripped command text; proven by deleting the pytest line while keeping the comment (2 red). (5) `tests/security/test_prompt_injection_fuzzing.py` passed `LLMConfig(enable_injection_filter=True)` — **a field that does not exist**, silently swallowed. Removed. (6) Generalised the orphan-tier pin: `TestEveryTierReachesCi` **discovers** `tests/*/` rather than using a hardcoded roster, with `_CI_EXEMPT_TIERS` (hardware only, documented). The first cut fixed the instances, not the class. (7) Catalog-wide hex-SHA gate in `test_harness_spec_aqa.py` — the rule was stated in HARNESS_SPEC and the openspec skill but enforced nowhere. (8) `make behaviour` target added (F-028's own description cited the Makefile gap). (9) Marker parity `-m "not hardware and not slow"`. (10) Loader-path env tests for the 3-level `MOUSEDROID_GCP__LOGGING__ENABLED` lever, previously untested. (11) `openspec/project.md` rows corrected from a false `implemented` to `in progress` — they have not merged.
**Post-review evidence:** `make gates` clean; `validate.py --tier fast` **OK 22 done / 20 ran**; `make test` 6108 passed, coverage 93.49%; regression+behaviour 832 passed; **changed-line coverage 100%** on both `gcp_cloud.py` (2 lines) and `factory.py` (6 lines).
**Copilot review round (5 findings on `4d026bf`, all valid, all fixed in `c858d94`):** (1) **`scripts/prove_pin_fails.sh` keyed its snapshot on `$(basename)`** — two `--paths` entries sharing a filename collided, the second overwriting the first, so restore wrote b's bytes over a. Data loss in a tool whose whole purpose is safe restore. Snapshots now mirror the sanitized relative path (leading `./`/`/` stripped, `..` neutralised); new `tests/unit/scripts/test_prove_pin_fails.py` drives the real script against a synthetic two-commit git repo — copied into `<tmp>/scripts/` so the script's own `cd $(dirname $0)/..` points at the fixture, needing no test-only hook in production code. Verified red against the reverted implementation (`pkg_a` came back holding `pkg_b`'s marker). (2) **Marker divergence across three sites** — `ci.sh` and `scripts/validations/F-028.sh` filtered `not hardware` while `ci.yml` filtered `not hardware and not slow`, so a green local run and a green validation command asserted a *different test set* than CI gated on. Latent only because no `slow` marks live in those tiers yet. Aligned **and pinned**: `TestOrphanTierMarkerParity` extracts the expression from each site (joining backslash continuations first) and requires both parity and that each still excludes `hardware` — parity alone is satisfiable by deleting the filter everywhere. F-028.sh now runs that class, since it is itself one of the sites. Self-caught while verifying: the first cut of that pin named the three sites from the review finding and missed a **fourth** — the `make behaviour` target added earlier in this same sprint, which agreed only by luck. That is the hardcoded-roster mistake in miniature, so the pin now *discovers* sites (every `ci.yml` job, `ci.sh`, the `Makefile`, and every `scripts/validations/*.sh`) and a companion assertion pins that the known sites are still found — otherwise dropping a site would let parity pass vacuously over the survivors. Both failure modes proven red. (3) **The catalog-wide hex-SHA gate's docstring overclaimed** — it said *resolvable*, it checks format. Corrected, with the limitation stated: `175606b0…` is well-formed, resolvable, and names the branch point containing neither change; it shipped past that very assertion earlier this session. No git check added — this repo is routinely shallow-cloned (`validate.py --tier fast` warns on F-002/004/005/006/007 here for exactly that reason), so resolvability stays with the nightly `--strict-git` run against a full clone. (4) **`test_twin_overlay_still_builds_its_cloud_components` asserted nothing** — it called both builders in a loop with no assertion, so it could only fail if one raised, while the comment above it stated the claim the code never checked. Both builders legitimately return `None` when the optional `[gcp]` extra is absent, making a return-value assertion untestable; it now asserts the **absence** of the `cloud_*_disabled` structured events, with a converse test pinning that a gate which does block stays diagnosable. Proven red by inverting the gate and by silencing it. (5) **The tool was never run against the pins shipped beside it.** Now dogfooded, and recorded as Phase 4 in both bundles' `tasks.md` so the next bundle inherits the discipline rather than the reminder.
**Dogfooding evidence (`scripts/prove_pin_fails.sh`, all `PROVE-PIN OK`):** F-028 wiring — reverting `scripts/ci.sh` + `.github/workflows/ci.yml` turns **5 of 20** assertions red (an earlier entry said 6 of 19, measured before the parity class was generalised; the two parity assertions now pass *vacuously* over the two sites the revert leaves standing, which is precisely why `test_the_known_wiring_sites_are_all_discovered` exists — and it fires); F-029 defaults — reverting `gcp_cloud.py` turns the pair red; F-029 factory gate — reverting `factory.py` turns the diagnostic-event test red (and confirms the pre-gate factory built a **live** `CloudTelemetrySink` from the twin overlay on an install carrying the `[gcp]` extra). Two shapes the tool cannot cover, now documented in the skill: catalog-wide invariant gates (green against a clean baseline by construction — proven instead by injecting a branch-name pin and an abbreviated SHA, both caught) and paths absent at the base ref.
**Copilot round 2 (3 findings on `84366b5`, all valid, all fixed):** the "tests/security/ is the only coverage of the pre-egress filter" claim — **refuted** two rounds ago (`tests/unit/security/test_injection_filter.py` holds 11 tests that already ran), corrected in five places with the note "corrected everywhere" — was still live in F-028's own `proposal.md` and `peer-review.md`, the latter marking it **CONFIRMED** in a verdict table. The earlier sweep fixed the places it remembered, not the places that had it; a re-sweep on whitespace-normalised whole-file text (a line-oriented grep misses the wrapped copy in `proposal.md`) found exactly the two Copilot named plus the mirror-image defect in `mouse-droid-cloud-egress-default-off/peer-review.md`, whose appendix listed `pubsub`/`storage` as unaudited when this change audits them. All three corrected. **Class fixed, not just the instances:** `TestOrphanTierNarrativeAccuracy` scans every live prose surface and requires any mention of the claim to be qualified (naming `test_injection_filter.py`, or "unit coverage") or marked refuted — framed that way rather than as a phrase blacklist because a verdict table *should* be able to quote the claim it refutes, and a proposal *should* be able to state the narrow true version. Proven red against the original wording. A governance record asserting a falsehood as **CONFIRMED** is worse than an uncorrected code comment: it is what the next engineer reads as settled.
**Copilot round 3 (1 finding on `4706efd`, 🔵 needs-a-closer-look, valid and NOT already mitigated):** F-028/F-029 pin `implemented_in` to branch-only commits that a squash merge will not preserve. My `notes:` fields already said "re-pin post-merge", and I had previously recorded that `scripts/archive_stale_branches.sh` "derives and protects pin carriers" — **that was about a different pin.** The script reads exactly one SHA, from `deployments/jetson-image.json`; all 15 `features.yaml` `implemented_in` pins had zero protection. So the hazard was real and unmitigated, and this PR was adding two more instances of it. Generalised the collection to both files, reusing the mechanism already there (tag-reachability check → carrier branches → protection lifts once a REMOTE tag covers the SHA). Extracted with `sed`, not a YAML parse, so it keeps working with no PyYAML — a protection that silently stops applying on an ImportError still prints a plan that looks complete; verified the extraction matches `yaml.safe_load` exactly (15/15). **Measured consequence, and it is large:** the cleanup plan moves from 67-to-delete/10-protected to **35/51**. Those 32 branches are the sole carriers of `implemented_in` SHAs — the old logic was going to destroy provenance for them, the same failure the deploy pin's protection exists to prevent and which `9c31968` already suffered once. Branch cleanup is now correctly contingent on the annotated-tag ritual, **which reshapes F-035**: the ritual must cover feature pins, not just the deploy manifest. New `tests/unit/scripts/test_archive_stale_branches.py` drives the real script in dry-run against a synthetic repo with a local bare remote (3 cases: pin protects its carrier, dropping the pin lifts it, a remote tag lifts it); 2 go red against the deploy-pin-only version.
**Next:** F-030 (doc reconciliation) per the approved plan; F-031 re-scoped to a disposition ADR after peer review found `AutonomousOrchestrator` calls an LLM inside the 30 Hz loop (CHARTER §3 / invariant 10) with zero safety machinery — hardware validation stays on `MouseDroidOrchestrator`.

## 2026-08-07 — Session 008
**Features worked:** F-025 (introduced todo in the same-branch reconciliation, now implemented → done); groundwork for F-026 (catalogued todo, untouched).
**Status changes:** F-025 → done (`implemented_in` = branch `claude/project-next-steps-p92so0`; swap to the squash SHA post-merge per the F-015..F-021 convention).
**Structural changes:** New codec seam `src/mousedroid/comms/command_set.py` (`ESP32CommandCodec` Protocol; stateless `LegacyCommandCodec` / `WaveshareStockCodec` singletons; `resolve_command_codec` with legacy fall-through) selected by the new `ESP32Config.command_set` Literal (+ `heartbeat_enabled` / `heartbeat_window_multiple` / `chassis_has_wheel_encoders`; after-validator derives stock 115200 baud only without an explicit pin and rejects stock+wifi at load). `BaseESP32Driver` delegates all protocol methods to the codec; both transports call the new `_arm_command_set()` at connect (chassis heartbeat under stock; zero extra writes under legacy — pinned); payload typing widened to covariant `Mapping[str, float]`. Factory logs `esp32_driver_built` with a `command_set` discriminator. Hardware smoke re-scoped for encoder-less chassis (audit R3) with keepalive re-send during settle; `jetson_smoke_test.sh` probe follows the selector. Vendor keys verified against `json_cmd.h` + `ugv_advance.h` (T=13 X/Z, T=136 cmd-ms, T=130 → T=1001 with `L`/`R`/`v`; no stock e-stop command).
**Validation evidence:** F-025 `validation_command` (`test_comms_utils.py` + `test_base_driver.py`) 76 passed; comms sweep 252 passed (5 pre-existing SSD1306 env failures on clean tree too); new `test_f025_backwards_compat.py` (24) + `test_f025_aqa.py` (15) + `test_f025_sanity.py` (7) + property additions green; `mypy --strict` clean on `comms/`; `ruff check` + `format --check` clean on touched files.
**Next:** operator bench: retest the "dead" board with `MOUSEDROID_ESP32__COMMAND_SET=waveshare_stock` (audit R2 — stock baud/protocol may explain the dead diagnosis) before repair-vs-replace; post-merge `implemented_in` re-pin; F-026 dead-knob cleanup; R4 IMU wiring consumes the same T=1001 frame.

## 2026-07-25 — Session 007
**Features worked:** Jetson deploy prep + full deployment/validation campaign plan (no F-number — operational prep; relates to F-008 hardware gate, F-018 trend, F-019 monitoring; branch `claude/jetson-deployment-validation-sdsn8q`).
**Structural changes:** (1) **`PYTHONOPTIMIZE=1` now actually set** in `Dockerfile.jetson` — a dozen docs and several `-O`-hardening code contracts asserted it as the Jetson default while no image set it; both in-container `ci.sh` execs in `jetson_full_validation.sh` now pass `-e PYTHONOPTIMIZE=0` so the pytest suite (which has never run under `-O`) keeps assert semantics. (2) **ESP32 safe-by-default** — `config/jetson_production.yaml` gains `esp32.enabled: false` (schema default untouched at `True`); the dead board previously crash-looped the container, and the repair lever is the `docker.env` env-flip. (3) **Strict pillar-skip gate** — additive `PillarResult.skip_reason` (`config_disabled`/`dry_run`/`environment`) + `--strict-skips` CLI flag + `MOUSEDROID_VALIDATION_PILLARS_STRICT_SKIPS` knob (default 1); `ARG INSTALL_DEV_TOOLS` aligned to compose's `true`. (4) **systemd unit fixed** — `Type=notify`+`WatchdogSec` against a non-notifying `docker compose up`, and a fatal local-only-image `pull` ExecStartPre, both of which prevented start. (5) **Docs** — config-resolution matrix (four launchers → four Settings), strict-skips contract, probe-first ESP32 rewrite, image-pin authority, legacy bare-metal annotation, stale `set -e` item deleted. (6) **Campaign plan doc** with WS-A/B/C, a C1 gate table incl. must-be-ABSENT signals, and a component-coverage appendix.
**Peer-review corrections that changed the design (5 lenses: technical, operational, governance, in-repo hardware docs, vendor docs):** a naive "fail on any pillar SKIP" gate would have false-failed every run (`memory`/`curiosity` are legitimately config-disabled on the prod overlay, and `--dry-run` skips all ten) → discriminate on `skip_reason`; the Phase-2 pillar step runs in the **host venv**, not the image, so the ARG flip is drift-consistency and the flag is the real fix; re-pinning `deployments/jetson-image.json` to a feature-branch SHA would recreate the `9c31968` gate-kill → re-pin is POST-MERGE only; commenting out `MOUSEDROID_TELEMETRY_TOKEN` would have *weakened* the `host_env_keys` check (parser skips `#` lines) → empty value, key retained; the face display IS validated (dedicated `oled` stage + hardware tier) — only the full-validation smoke loop lacks the entry; vendor stock ESP32 firmware speaks 115200 vs this repo's custom 1 M baud, so a stock reflash during repair would read as a still-dead board (now a C3 gate step); a firmware-dead ESP32 still enumerating is expected, not lucky (CP2102N bridge is bus-powered).
**Validation evidence:** new `tests/regression/test_jetson_deploy_prep_backwards_compat.py` 12 passed; `test_systemd_unit.py` 13 passed (3 new negative pins); `test_validate_pillars_cli.py` 11 passed (4 new strict-skips cases); `tests/unit/validation/test_pillars.py` 14 passed (4 new skip_reason cases); pre-existing pillar suites (21) green after the additive field. Full `scripts/ci.sh` + `-O` posture smoke run before push.
**Next:** operator executes WS-B (deploy) then WS-C (campaign) per the plan doc; post-merge re-pin of `deployments/jetson-image.json` to the trunk merge commit; deferred follow-ons — Piper `scout`/`friendly` voice staging + voice-latency benchmark, `nvidia-l4t-gstreamer` for the proper in-container CSI path, `oled` entry in the full-validation smoke loop.

## 2026-07-25 — Session 006
**Features worked:** Code-hygiene sprint (no F-number — cross-cutting hygiene, PR #178, branch `claude/code-hygiene-plan-0gtr93`).
**Structural changes:** (1) **Correctness** — fixed the mission-dedup follower path swallowing `asyncio.CancelledError` via an `(CancelledError, Exception)` tuple (500 instead of propagation) and the false "py3.10 CancelledError subclasses Exception" comment that motivated it (BaseException since 3.8; the claim had already propagated into a test docstring); replaced `-O`-stripped asserts in `retry.py` exhaustion flow + `main.py` entrypoint with real branches; `mock_settings` fixture no longer leaks `os.environ`. (2) **Gate integrity** — anchored the unanchored `"pass"`/`"\.\.\."` coverage exclude regexes (measured: 93.57%→93.48%, gate untouched); wired `tests/smoke` (152 tests, ran in NO CI path) into ci.yml + ci.sh; `tests/performance` as tracked advisory job; new `local-gates` job (settings identity, hooks mypy, skill validator, doc hygiene, workforce coverage); un-swallowed `pip-audit --strict` (it had failed on the editable local package EVERY run behind `|| echo` — fixed with `--skip-editable` and tracked in advisory_stages.yaml); `--import-mode=importlib` into pytest addopts; corrected the dead `-m slow` CI-invocation docstring claim; completed the runbook inventory pin (9→11). (3) **Cleanup** — deleted 15 grep-verified orphans (7 scripts, 4-file monitoring stack, jetson_setup.yaml, docs/prd/ ×3) + 2 analysis docs; moved `launch_dashboard.ps1` → `scripts/`; untracked the gitignore-matching jetson_validate report; stripped 34 inert `pylint: disable` comments. (4) **Ratchets** — mypy override-list budget (25 modules pinned), src/-targeting per-file-ignores budget (closes the documented inline-budget bypass), coverage-threshold single-source test.
**Survey claims rejected during verification (recorded so they aren't re-litigated):** `docs/jetson-runner-setup.md` slugs match the live GitHub remote (correct as-is); `progress.md` H1 is contractually excluded from the brand sweep; `smoke-reports/` is load-bearing for the workforce hooks; `run_training_pipeline.ps1` has live `.vscode/tasks.json` referrers; the operations/operator smoke-runbook pair is a documented intentional split (merge already tracked); the e2e/performance conftest "duplication" is irreducible per-directory pytest plumbing over shared `tests/_jetson_hardware.py` helpers.
**Validation evidence:** 5,866 passed / 93.48% coverage (unit+property+integration, anchored excludes); smoke 152 passed in 7.5 s; 6,809 tests collect clean under importlib; `mypy --strict` clean (329 files); ruff (pinned 0.8.0) + format clean; all new local-gates commands verified before wiring (workforce coverage 98.09%); tripwire regression suite green after each deletion batch.
**Next:** watch PR #178 CI to green; follow-up PRs scoped in the plan: shared SHA-256 slot-store base extraction, `factory.py`/`schema.py` megafile splits (each needs its CLAUDE.md invariant + source-text pin amended in the same PR), `metrics.py` split assessment; vulture triage round before the ~2026-10 promotion decision.

## 2026-07-24 — Session 005
**Features worked:** F-024 (Claude Code workforce governance) — OpenSpec change `mouse-droid-claude-workforce`, branch `claude/workforce-modernization-review-5tf7y3`, PR #176.
**Status changes:** F-024 introduced as `in_progress` (Phases 1–2 of the bundle; implemented_in pins to the squash SHA post-merge).
**Structural changes:** (1) **Peer review + spec rewrite** — a user-supplied draft bundle was fact-checked against the tree and rewritten as rev. B; 14 evidence-cited verdicts in `openspec/changes/mouse-droid-claude-workforce/peer-review.md`. The draft's central premise was inverted (it claimed only `sim-test` was frozen; all three have been frozen since PR #151), two of its mechanisms would have failed existing gates (`status: legacy` is rejected by `validate_skill_commands.py`; recreating `.claude/commands/` breaks `test_legacy_commands_dir_stays_deleted`), and several enforcement claims were stale (gitleaks shipped as F-015; `PLR2004` not enabled; coverage/mypy/GitHub-lint scopes exclude `tools/`; no `openspec` CLI exists here). (2) **Implementation** — `.claude/workforce.yaml` + `tools/claude_hooks/` (8 modules): three hooks (edit-time gitleaks scan, F-008-keyed capability freeze gate that self-disables, advisory post-edit checks) over four reusable primitives (config/paths/hookio/logging_setup + portability). Hooks log to **stderr** (stdout is the decision channel) and never import `mousedroid`. (3) **Gates** — `tests/regression/test_claude_workforce_aqa.py`, three new `scripts/ci.sh` stages (config parse, `mypy --strict tools/claude_hooks/`, dedicated `--cov=tools/claude_hooks --cov-branch`), and `ci.yml` lint scope extended to `tools/`.
**Defects caught by this work's own testing:** hook commands were unrunnable as first written (`python3 path/to/hook.py` leaves the repo root off `sys.path` → `ModuleNotFoundError` on every edit; fixed to `cd "$CLAUDE_PROJECT_DIR" && python3 -m …`); `structlog.configure()` mutated process-global state the unit conftest pins (switched to `wrap_logger`); **`.gitignore` excluded all of `.claude/`, so `workforce.yaml` never shipped** — green locally, absent in CI, caught by `test_repo_config_file_is_valid` and now pinned by `test_shared_claude_assets_are_git_tracked`.
**Doc reconciliation (same session):** CLAUDE.md (new F-024 surface section; "CI Pipeline (5 stages)" corrected against the real 14-job `ci.yml`; testing/config/structure/commands scopes), AGENTS.md, SKILLS.md, `agent.md`, HARNESS_SPEC.md, `docs/CHARTER.md`, `docs/testing.md`, `docs/development.md`, `docs/README.md`, `tools/README.md`, NEXT_STEPS.md, new `docs/architecture/c4-claude-workforce.md` + three `c4-overview.md` tables, ADR-012 stage-count conflict. Corrected the false "85% **branch** coverage" claim in README, `docs/testing.md`, `tests/README.md`, and the PR template — branch coverage is measured only for `tools/claude_hooks/`, and advisory.
**Validation evidence:** `bash scripts/ci.sh` green through the workforce stages; 5817 passed / 93.57% coverage on the full unit+property+integration tier (the single failure was `test_tick_count_after_5_seconds`, a wall-clock 30 Hz test that passes 13/13 in isolation and failed only under concurrent-suite CPU contention); hook package 200 tests at ~98% line with branch measured; `mypy --strict` clean on 329 src + 9 hook files; ruff clean; skill validator + harness fast tier green.
**Next:** Phases 3–6 of the bundle (subagent roster, five new skills, secretless `.mcp.json` incl. the repo's own MCP server, CLAUDE.md restructure); promote the advisory `gitleaks` job once the 7-green-run tracker clears; post-merge `implemented_in` SHA swap.

## 2026-07-23 — Session 004
**Features worked:** F-023 (AlayaWorld-adapted bounded-context memory + drift-reduction training + distillation spike) — OpenSpec change `mouse-droid-alayaworld-memory-distill`, branch `claude/mouse-droid-alayaworld-adapt-dt07mp`.
**Status changes:** F-023 introduced as `in_progress` (implemented_in pins to the squash SHA post-merge).
**Structural changes:** WS1 — OpenSpec archive (`openspec/project.md` + `changes/<id>/` with proposal/tasks/design + two ADDED-requirement spec deltas carrying the declared deviations), design spec (3-agent adversarial peer review incorporated), plan, ADR-015, new `docs/related-work.md` (adaptation-not-adoption + no-equivalence disclaimer), `WorldModelMemoryConfig` (Settings-level, default-OFF) + `DriftTrainingConfig` (`training.drift`, default-OFF). WS2 — `world_model/bounded_context.py` (sink + ring + EMA, NaN contract, cold-start identity, mission/OTA sink lifecycle), `build_latent_context`, orchestrator observe-seam wiring behind a `healthy` flag from `_validate_latent`. WS3 — `_posterior_step` refactor + `train_sequence_corrupted` (k=0 allclose contract, private prefix Generator) + eval-only `DriftCorrectionHead`, `training/drift_metrics.py` (range-headline per-modality open-loop drift + latent divergence), `training/drift_reduction.py`, `scripts/compare_drift.py`. WS4 — `scripts/spike_step_distillation.py` (deterministic prior-mean k-step teacher, discounted return, primitive-vs-consumer-ceiling framing), Jetson spike runbook. WS5 — in-container seeded comparison + spike numbers into `docs/analysis/`.
**Validation evidence:** F-023 validation_command (10 new test files) green; RSSM golden suites + on-device suite green (train_sequence refactor behavior-pinned); ruff + mypy --strict + full `scripts/ci.sh`-equivalent green pre-push.
**Next:** operator Jetson spike run per `docs/runbooks/jetson-alayaworld-spike.md`; real-replay drift re-run; keep `world_model_memory.enabled` off pending soak.

## 2026-07-03 — Session 003
**Features worked:** F-015 (secret-scan gate, WS-0.4), F-016 (truth reconciliation, WS-1), F-017 (host-env durability, WS-3.1), F-018 (validation trend instrumentation, WS-4), F-019 (LLM observability, WS-5), F-020 (redundancy/gap audit, WS-8) — rev. B validation-first plan, PR #151.
**Status changes:** F-015..F-020 → done (branch provenance; swap to squash SHA post-merge).
**Structural changes:** F-015 — `.gitleaks.toml` (regex-only allowlist), advisory `gitleaks` CI job (docker-pinned, full-history; first run surfaced + allowlisted the synthetic telemetry-auth test token), guarded `scripts/ci.sh` stage, `docs/runbooks/secret-scanning.md`. F-016 — NEXT_STEPS.md 37 KB/72 ✅ → ~12 KB/4 ✅ (landed work → CHANGELOG "Historical record"; Claude-Code runbook → `docs/runbooks/claude-code-on-jetson.md`), arm arc PAUSED at T2 with unfreeze condition, Phase-5 vocabulary disambiguated across both NEXT_STEPS files, 3 skills frozen via validated `status:` front-matter (`validate_skill_commands.py` gained the `invalid-status` check), `tools/doc_hygiene.py` advisory guard wired into ci.sh.
**Validation evidence:** `python -m pytest tests/regression/test_secret_scan_gate.py tests/unit/tools/test_doc_hygiene.py tests/regression/test_next_steps_reconciled.py -q` green; `bash scripts/ci.sh` green pre-commit per commit.
**Gap-analysis pass (same session):** two scan agents audited the branch (tech-debt: 0 HIGH / 6 MED / 6 LOW; doc-drift sweep). All MED + cheap LOW fixed (malformed-metadata tolerance incl. Copilot's file-level cases, `.yaml` workflow glob, fail-safe journal rotation, dead constants, `if-no-files-found`); 16 new tests execute the previously source-text-only paths (bootstrap rollback/timer dry-runs, python-less `write_summary_fallback`). Docs reconciled: README, **ADR-013** (F-namespaces + findings-only audit posture) + ADR-012 addendum, C4 validation-efficiency/overview/llm-gateway, both Jetson runbooks, CLAUDE.md, SKILLS.md, ignores; new `test_runbooks_structure.py` pins the 8-runbook inventory.
**Next:** hardware gate (F-008): ESP32 bench repair per NEXT_STEPS item 2; post-merge `implemented_in` SHA swap.

## 2026-06-15 — Session 002
**Features worked:** Harness hardening (no catalog status changes).
**Status changes:** none.
**Structural changes:** Gap analysis + tech-debt resolution on `claude/harness-spec-template-g3inh7`. Extracted the harness enforcement logic (schema/DAG/provenance/tier-gating/selection) out of `scripts/` into a new importable, mypy-strict, dependency-injectable module `src/mousedroid/harness/spec.py`; reduced `scripts/validate.py` + `scripts/select_next.py` to thin CLI shims (identical CLI contract, now CWD-robust). Added `tests/unit/harness/test_spec.py` (28 tests, 100% on `spec.py`) so the harness guarantees fall under the 85% coverage gate. Updated `test_harness_spec_aqa.py` to import the single canonical `check_dag` (removed the duplicated cycle-detection DFS).
**ADRs:** Updated ADR-012 (importable-module consequence + scope).
**Validation evidence:** `python scripts/validate.py --tier fast` exits 0 (incl. from `/tmp`); `tests/unit/harness/ + regression` → 142 passed; `ruff check` + `ruff format --check` clean; `python -m mypy --strict src/mousedroid/harness/spec.py` clean; `coverage` on `spec.py` = 100%.
**Next:** Same post-merge provenance maintenance (F-001/F-003 branch refs → squash SHA). Confirm the full `scripts/ci.sh` coverage gate ≥85% in CI.
