{
  "platform": "jetson",
  "image_tag": "mousedroid:jetson",
  "sha": "9c3196885bd87f59c95ee86ae1eb1af077f648e9",
  "deployed_at": "2026-06-02T20:35:32Z",
  "notes": "Rebuilt on the rover from feat/jetson-claude-pilot-deploy @ 9c31968 (PR #111). Bakes the PR #107 LLMConfig schema (backend Literal gains 'anthropic'; new fallback_backend / fallback_model_name / fallback_retry_cooldown_s fields) AND the anthropic SDK via Dockerfile Stage 4b (which installs the 'anthropic>=0.40' lower-bound range; version 0.105.2 resolved at this build), so the cloud LLM tier now survives `docker compose up -d --force-recreate` WITHOUT a manual hot-install (closes the prior writable-layer SDK-wipe gap). Verified post-recreate on this SHA's image: container healthy, anthropic_gateway_started with primary_ready=true (api_key_source=env), Phi-3 fallback secondary_ready=true, reactive 30 Hz loop unaffected. The CI config-schema-compat gate now validates config/*.yaml against THIS SHA's Settings schema (which includes the PR #107 LLMConfig fields). Per-host docker.env overrides live on the rover only (not committed): MOUSEDROID_LLM__ENABLED=true and MOUSEDROID_LLM__N_GPU_LAYERS=0 (the Phi-3 fallback runs on CPU because the world model occupies the shared iGPU). Update this file in the same PR (or immediately after) whenever the Jetson image is rebuilt."
}
