{
  "platform": "jetson",
  "image_tag": "mousedroid:jetson",
  "sha": "032942b50ab71abf285282a4de7333193f208c38",
  "deployed_at": "2026-06-02T22:52:57Z",
  "notes": "Re-pinned to the integration tip 032942b (PRs #110/#111/#112 merged into claude/markdown-implementation-plan-aVJ2l). The prior pin 9c31968 was the squash-source feature commit, which became unreachable after the feature branch was deleted post-merge \u2014 and the CI config-schema-compat gate worktrees out this SHA, so it must be a permanent, reachable commit. 032942b is schema-equivalent for config validation (jetson_production.yaml validates cleanly against it; the 9c31968->032942b schema.py delta is comments/formatting + #110 voice fields, no field removals affecting the rover config). The rover runs the bind-mounted source at /opt/mousedroid (editable install), re-pointed to this integration tip, so its EFFECTIVE Settings schema is 032942b; the baked image layer (built 2026-06-02T20:35 from 9c31968 \u2014 bakes the PR #107 LLMConfig schema AND the anthropic SDK via Dockerfile Stage 4b, 'anthropic>=0.40' range, 0.105.2 resolved) is shadowed by that bind-mount but still supplies the baked anthropic SDK so the cloud tier survives --force-recreate. Per-host docker.env overrides live on the rover only (not committed): MOUSEDROID_LLM__ENABLED=true and MOUSEDROID_LLM__N_GPU_LAYERS=0 (Phi-3 fallback on CPU; the world model occupies the shared iGPU). Update this file whenever the image is rebuilt or the rover's tracked source SHA changes. REACHABILITY (corrected 2026-08-28, F-035): this file previously asserted the SHA was 'kept reachable by the annotated tag deployments/jetson-image-032942b, NOT by any branch', and separately that it had been 'reachable only from six stale feature branches'. The first claim was aspirational and is false: `git ls-remote --tags origin` returns ZERO tags, so that tag has never existed. The second was true when written but is now stale -- `git branch -r --contains` reports 10 carrier branches as of 2026-08-28. Current state, verified: the SHA is NOT an ancestor of the default branch, no tag anywhere references it, and its only reachability is those 10 stale branches -- precisely the set scripts/archive_stale_branches.sh exists to delete, which is why the config-compat CI gate that worktrees this SHA out is one cleanup away from dying repo-wide. REMEDY: run `bash scripts/repin_tags.sh` for the plan and `--push` (an operator action, separately confirmed) to create the tag; only then does the original sentence become true. When re-pinning to a new SHA, tag it BEFORE deleting any branch that carries it, and do not rely on branch reachability."
}
