# MouseDroidAGI v0.4.0+ Next Steps Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore CI green (3 mypy errors + 4 test collection errors), close test gaps across 7 untested modules, add property-based tests, then advance the training pipeline, CfC maturation, observability, and continual learning roadmap items -- all with TDD, backwards-compatible config, protocol-based DI, and 85%+ coverage maintained.

**Architecture:** All work follows MouseDroidAGI's 9 invariants: protocol-based DI, factory.py single wiring point, no hardcoded values (Pydantic config from YAML), structlog logging, asyncio-only, mypy --strict, torch.no_grad() for inference, deque(maxlen=N) ring buffers, backwards-compatible config defaults.

**Tech Stack:** Python 3.10+, PyTorch, Pydantic v2, structlog, ncps (CfC), pytest + hypothesis + pytest-asyncio, ruff, mypy --strict, Docker/Jetson L4T

---

> Full plan content is in the Claude Code plan file. This is a pointer for project history.
> Plan file: `.claude/plans/temporal-sprouting-blum.md`
> Date: 2026-04-15

## Phases

| Phase | Focus | Priority | Effort |
|-------|-------|----------|--------|
| 0: CI Health | Fix 3 mypy errors + 4 test collection errors | CRITICAL | 30-45 min |
| 1: Test Gaps | Coverage for 7 untested modules | HIGH | 2-3 hrs |
| 2: Code Quality | Hypothesis property tests + memory consolidation | HIGH | 1.5-2 hrs |
| 3: Training Pipeline | RSSM, MCTS, BDI, Constitutional RL (P2) | MEDIUM | 2-3 weeks |
| 4: CfC Maturation | Time-delta, attention fusion, TensorRT (P8) | MEDIUM | 3-4 weeks |
| 5: Observability | Grafana, Loki, telemetry security (P5) | LOW | 3-5 days |
| 6: Continual Learning | EWC + PNN training script (P6.5) | LOW | 1 week |
